<!doctype html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, member-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet"
          as="style"
          crossorigin
          href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable-dynamic-subset.min.css"
    />
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        function formatDate(isoString) {
            const date = new Date(isoString);
            const year = date.getFullYear();
            const month = String(date.getMonth() + 1).padStart(2, '0');
            const day = String(date.getDate()).padStart(2, '0');
            const hours = String(date.getHours()).padStart(2, '0');
            const minutes = String(date.getMinutes()).padStart(2, '0');
            return `${year}-${month}-${day} ${hours}:${minutes}`;
        }
    </script>
    <style>
        body {
            font-family: 'Pretendard', sans-serif;
        }
    </style>
    <title>Shopping Mall</title>
</head>
<body class="bg-slate-50">
<header class="flex justify-center w-full px-5 py-2 shadow">
    <div class="flex max-w-[1200px] h-[50px] w-full justify-between items-center">
        <a href="/">
            <h1 class="text-amber-400 font-bold text-2xl">Shopping Mall</h1>
        </a>
        <ul class="flex gap-5">
            {{#isLoggedIn}}
                <li><a class="text-black" href="/order">주문내역</a></li>
                <li><a class="text-black" href="/myProducts">담은상품</a></li>
                <li><a class="text-black" href="/tickets/myTickets">문의내역</a></li>
                <li><a class="text-black" href="/myInfo">내 프로필</a></li>
                <li><a class="text-black" href="/logout">로그아웃</a></li>
            {{/isLoggedIn}}
            {{^isLoggedIn}}
                <li><a class="text-black" href="/login">로그인</a></li>
            {{/isLoggedIn}}
        </ul>
    </div>
</header>
